<Page
    x:Class="SharpSolutions.JIPA.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SharpSolutions.JIPA.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:SharpSolutions.JIPA.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance vm:HomeViewModel}"
    d:DesignHeight="480"
    d:DesignWidth="800"
    Loaded="OnPageLoaded"
    >

    <Page.DataContext>
        <vm:HomeViewModel/>
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*" />
                <RowDefinition Height="3*" />
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0">
                <TextBlock Grid.Column="1"  FontSize="50" TextAlignment="Center" Text="{Binding Time.Time}" Width="Auto" />
                <TextBlock Grid.Column="1"  FontSize="25" TextAlignment="Center" Text="{Binding Time.Date}" Width="Auto" />
            </StackPanel>
            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="5"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Grid.Row="0"  FontSize="25" TextAlignment="Left" Text="Temp:" Width="Auto" />
                <TextBlock Grid.Column="0" Grid.Row="1"  FontSize="25" TextAlignment="Left" Text="Pres:" Width="Auto" />
                <TextBlock Grid.Column="2" Grid.Row="0"  FontSize="25" TextAlignment="Right" Text="{Binding Temperature.Temperature}" Width="Auto"  />
                <TextBlock Grid.Column="2" Grid.Row="1"  FontSize="25" TextAlignment="Right" Text="{Binding Temperature.Pressure}" Width="Auto" />
            </Grid>
        </Grid>

    </Grid>
</Page>
