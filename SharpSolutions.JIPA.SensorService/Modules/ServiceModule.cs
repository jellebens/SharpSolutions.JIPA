using Autofac;
using SharpSolutions.JIPA.SensorService.Services;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Reflection;
using System.Text;
using System.Threading.Tasks;

namespace SharpSolutions.JIPA.SensorService.Modules
{
    //public sealed class ServiceModule : Autofac.Module
    //{
    //    protected override void Load(ContainerBuilder builder)
    //    {
    //        Assembly asm = typeof(IService).GetTypeInfo().Assembly;

    //        builder.RegisterAssemblyTypes(asm).Where(t => t.IsAssignableTo<IService>());
    //    }
    //}
}
